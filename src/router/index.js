import Vue from 'vue'
import Router from 'vue-router'

Vue.use(Router);




//创建路由对象
var router = new Router({

  routes:[

  ]
});

//把路由对象暴露出去
export default router;


export default new Router({
  routes: [

  ]
})
